## Summary

<!--
Lead with a one-line framing bullet, then detail bullets. Use
`- **Bold intro** — description` for each substantive change; add a
`### Subsection` heading when the PR spans distinct concerns
(correctness / templates / tooling / adjacent cleanup).
-->

-

### Related PRs

<!--
List PRs across sibling repos that land together (engine / theme / site).
Remove this section if the change is self-contained to one repo.
-->

-

## Test plan

<!--
Checklist of verification steps actually run locally. Include the exact
commands so reviewers see what's been covered. Typical entries:

- [ ] `cargo fmt --all --check` — clean
- [ ] `cargo clippy --all-targets -- -D warnings` — zero warnings
- [ ] `cargo test` — all tests pass
- [ ] `cargo llvm-cov --ignore-filename-regex 'main\.rs'` — coverage ≥ threshold
- [ ] End-to-end on hakula.xyz-kiln — feature renders correctly
-->

- [ ]
